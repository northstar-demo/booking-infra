name: booking-terraform

on:
  pull_request:
    paths:
      - "environments/dev/**"
      - ".github/workflows/terraform.yml"
      - "demo-app/**"
  push:
    branches:
      - main
    paths:
      - "environments/dev/**"
      - ".github/workflows/terraform.yml"
      - "demo-app/**"

env:
  APP_NAME: booking
  DEPLOY_ENV: dev
  TF_WORKING_DIR: environments/dev

jobs:
  validate:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: environments/dev
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Allow Terraform module checkout from GitHub
        run: git config --global url."https://x-access-token:${{ secrets.GH_MODULES_TOKEN }}@github.com/".insteadOf "https://github.com/"

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform fmt
        run: terraform fmt -check -recursive

      - name: Terraform init without backend
        run: terraform init -backend=false

      - name: Terraform validate
        run: terraform validate

  deploy:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    defaults:
      run:
        working-directory: environments/dev
    env:
      AWS_REGION: ${{ vars.AWS_REGION }}
      AWS_ACCOUNT_ID: ${{ vars.AWS_ACCOUNT_ID }}
      TF_STATE_BUCKET: ${{ vars.TF_STATE_BUCKET }}
      TF_LOCK_TABLE: ${{ vars.TF_LOCK_TABLE }}
      FOUNDATION_STATE_BUCKET: ${{ vars.TF_STATE_BUCKET }}
      FOUNDATION_LOCK_TABLE: ${{ vars.TF_LOCK_TABLE }}
      TF_STATE_KEY: booking/dev/terraform.tfstate

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Allow Terraform module checkout from GitHub
        run: git config --global url."https://x-access-token:${{ secrets.GH_MODULES_TOKEN }}@github.com/".insteadOf "https://github.com/"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          role-session-name: github-actions-booking
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform fmt
        run: terraform fmt -check -recursive

      - name: Terraform init
        run: >
          terraform init
          -backend-config="bucket=${TF_STATE_BUCKET}"
          -backend-config="key=${TF_STATE_KEY}"
          -backend-config="region=${AWS_REGION}"
          -backend-config="dynamodb_table=${TF_LOCK_TABLE}"

      - name: Terraform validate
        run: terraform validate

      - name: Terraform plan
        run: >
          terraform plan
          -var="region=${AWS_REGION}"
          -var="environment=${DEPLOY_ENV}"
          -var="foundation_state_bucket=${FOUNDATION_STATE_BUCKET}"
          -var="foundation_lock_table=${FOUNDATION_LOCK_TABLE}"
          -var="container_image=${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/northstar-demo-${APP_NAME}:${GITHUB_SHA}"
          -out=tfplan

      - name: Terraform apply bootstrap
        run: terraform apply -auto-approve tfplan

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push demo image
        working-directory: demo-app
        env:
          REPOSITORY_URL: ${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/northstar-demo-booking
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t "${REPOSITORY_URL}:${IMAGE_TAG}" .
          docker push "${REPOSITORY_URL}:${IMAGE_TAG}"

      - name: Terraform apply final image
        run: >
          terraform apply -auto-approve
          -var="region=${AWS_REGION}"
          -var="environment=${DEPLOY_ENV}"
          -var="foundation_state_bucket=${FOUNDATION_STATE_BUCKET}"
          -var="foundation_lock_table=${FOUNDATION_LOCK_TABLE}"
          -var="container_image=${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/northstar-demo-${APP_NAME}:${GITHUB_SHA}"
