server {
  listen 80;
  server_name _;

  location / {
    root /usr/share/nginx/html;
    try_files /index.html =200;
  }

  location = /booking/health {
    add_header Content-Type text/plain;
    return 200 "booking-ok\n";
  }
}
